[pytest]
markers =
    integration: marks tests as integration tests that use real external services like email
    component: marks tests as component tests that can run without external services
testpaths = tests/component tests/integration
timeout = 30
